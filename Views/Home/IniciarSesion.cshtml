@*@{
    ViewData["Title"] = "Iniciar Sesión";
}
<head>
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<div class="container">
    <h1>Inicie sesión para comenzar</h1>
    <form method="post" action="/Home/Ingresar">
        Nombre de Usuario:
        <input type="text" name="Username" placeholder="Ingresar usuario" required /><br />
        Contraseña:
        <input type="password" name="password" required /><br />
        <input type="submit" value="Ingresar" />
    </form>

    @if (ViewBag.Error != null)
    {
        <p>@ViewBag.Error</p>
    }
</div>
*@

@{
    ViewData["Title"] = "Iniciar Sesión";
}
<head>
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<main class="centered">
    <div class="card">
        <h1>Inicie sesión para comenzar</h1>
        <form method="post" action="/Home/Ingresar" class="form">
            <label>Nombre de Usuario:</label>
            <input type="text" name="Username" placeholder="Ingresar usuario" required />
            <label>Contraseña:</label>
            <input type="password" name="password" required />
            <input type="submit" value="Ingresar" class="btn" />
        </form>

        @if (ViewBag.Error != null)
        {
            <p class="error">@ViewBag.Error</p>
        }
    </div>
</main>

